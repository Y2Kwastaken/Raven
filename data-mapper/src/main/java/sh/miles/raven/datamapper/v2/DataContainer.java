package sh.miles.raven.datamapper.v2;

import sh.miles.raven.datamapper.v2.data.DataField;
import sh.miles.raven.datamapper.v2.data.DataTuple;

/**
 * Markup interface for classes that can represent a set of data fields that want to represent data should have
 * {@link DataField} or {@link DataTuple}
 */
public interface DataContainer {
}
